---
- name: Test custom Ansible virtualenv
  hosts: localhost
  gather_facts: false

  tasks:

    - name: Show Python executable
      ansible.builtin.command:
        cmd: python3 -c "import sys; print(sys.executable)"
      register: python_exe
      changed_when: false

    - name: Show Python version
      ansible.builtin.command:
        cmd: python3 --version
      register: python_version
      changed_when: false

    - name: Show Ansible version
      ansible.builtin.command:
        cmd: ansible --version
      register: ansible_version
      changed_when: false

    - name: Show Ansible core version directly
      ansible.builtin.command:
        cmd: python3 -c "import ansible; print(ansible.__version__)"
      register: ansible_core
      changed_when: false

    - name: Show pyVmomi version
      ansible.builtin.command:
        cmd: python3 -c "from importlib.metadata import version; print(version('pyVmomi'))"
      register: pyvmomi_version
      changed_when: false

    - name: Display environment
      ansible.builtin.debug:
        msg:
          - "Python executable: {{ python_exe.stdout }}"
          - "Python version: {{ python_version.stdout }}"
          - "Ansible core: {{ ansible_core.stdout }}"
          - "pyVmomi: {{ pyvmomi_version.stdout }}"
